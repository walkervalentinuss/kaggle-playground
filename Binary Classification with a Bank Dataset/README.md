# Kaggle Playground S5E8 - Binary Classification with a Bank Dataset

setup
open anaconda cmd
conda info --envs # check available env, if 0, create 1 (not recommended doing project in base env)
conda create --name myenv python=3.9 numpy pandas scipy # create conda env with python version desired and library you need
conda activate myenv # activate the env
conda deactivate # deactivate the env